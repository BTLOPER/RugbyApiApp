<Window x:Class="RugbyApiApp.MAUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RugbyApiApp.MAUI.Converters"
        xmlns:iconPacks="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Material"
        Title="Rugby API Manager" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        FontFamily="Segoe UI"
        FontSize="12">

    <Window.Resources>
        <!-- Colors -->
        <Color x:Key="PrimaryColor">#512BD4</Color>
        <Color x:Key="PrimaryDarkColor">#3D1F8A</Color>
        <Color x:Key="SecondaryColor">#DFD8F7</Color>
        <Color x:Key="AccentColor">#00B4D8</Color>
        <Color x:Key="SuccessColor">#22C55E</Color>
        <Color x:Key="DangerColor">#EF4444</Color>
        <Color x:Key="BackgroundColor">#F5F5F5</Color>
        <Color x:Key="SurfaceColor">#FFFFFF</Color>
        <Color x:Key="TextColor">#1F1F1F</Color>
        <Color x:Key="TextSecondaryColor">#757575</Color>
        <Color x:Key="BorderColor">#E0E0E0</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#512BD4"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#3D1F8A"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#DFD8F7"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#00B4D8"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#22C55E"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#EF4444"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextBrush" Color="#1F1F1F"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#757575"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E0E0E0"/>

        <!-- Button Styles -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
        </Style>

        <!-- TextBlock Styles -->
        <Style x:Key="HeadingStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style x:Key="SubHeadingStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style x:Key="BodyStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- TabItem Style -->
        <Style x:Key="ModernTabItem" TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,3"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter ContentSource="Header" Margin="{TemplateBinding Padding}" HorizontalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SecondaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="RowBackground" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
        </Style>

        <!-- GroupBox Style -->
        <Style x:Key="ModernGroupBox" TargetType="GroupBox">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <!-- PasswordBox Style -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <!-- Rating Star Color Converter -->
        <local:RatingStarColorConverter x:Key="RatingStarColorConverter"/>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="163*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.RowSpan="2">
            <!-- Header -->
            <Border DockPanel.Dock="Top" Background="{StaticResource PrimaryBrush}" Padding="24,16">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <iconPacks:PackIconMaterial  Kind="Rugby"  Width="32" Height="32" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Rugby API Manager" Style="{StaticResource SubHeadingStyle}" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Content -->
            <TabControl Background="{StaticResource BackgroundBrush}" BorderThickness="0" x:Name="TabControl">
                <!-- Home Tab -->
                <TabItem Style="{StaticResource ModernTabItem}" x:Name="HomeTab">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <iconPacks:PackIconMaterial Kind="Home"  Width="16" Height="16" Margin="0,0,4,0"/>
                            <TextBlock Text="Home"/>
                        </StackPanel>
                    </TabItem.Header>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
                        <StackPanel>
                            <!-- Welcome Section -->
                            <TextBlock Text="Rugby Data Dashboard" Style="{StaticResource HeadingStyle}" Margin="0,0,0,8"/>
                            <TextBlock Text="Real-time statistics of your rugby database" Style="{StaticResource BodyStyle}" Margin="0,0,0,32"/>

                            <!-- Data Overview Grid -->
                            <Grid Margin="0,0,0,24">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Countries Card -->
                                <Border Grid.Row="0" Grid.Column="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,12,12">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                            <iconPacks:PackIconMaterial  Kind="Earth"  Width="24" Height="24" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Countries" Style="{StaticResource SubHeadingStyle}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding CountriesPercent}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,8"/>
                                        <ProgressBar Height="8" Background="#E8E8E8" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,8" Value="{Binding CountriesProgress}"/>
                                        <TextBlock Text="{Binding CountriesPercent}" Style="{StaticResource BodyStyle}"/>
                                    </StackPanel>
                                </Border>

                                <!-- Seasons Card -->
                                <Border Grid.Row="0" Grid.Column="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="12,0,0,12">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                            <iconPacks:PackIconMaterial  Kind="Calendar" Width="24" Height="24" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Seasons" Style="{StaticResource SubHeadingStyle}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding SeasonsPercent}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>
                                        <ProgressBar Height="8" Background="#E8E8E8" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8" Value="{Binding SeasonsProgress}"/>
                                        <TextBlock Text="{Binding SeasonsPercent}" Style="{StaticResource BodyStyle}"/>
                                    </StackPanel>
                                </Border>

                                <!-- Leagues Card -->
                                <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,12,12,12">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                            <iconPacks:PackIconMaterial  Kind="Trophy" Width="24" Height="24" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Leagues" Style="{StaticResource SubHeadingStyle}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding LeaguesPercent}" FontSize="24" FontWeight="Bold" Foreground="#F59E0B" Margin="0,0,0,8"/>
                                        <ProgressBar Height="8" Background="#E8E8E8" Foreground="#F59E0B" Margin="0,0,0,8" Value="{Binding LeaguesProgress}"/>
                                        <TextBlock Text="{Binding LeaguesPercent}" Style="{StaticResource BodyStyle}"/>
                                    </StackPanel>
                                </Border>

                                <!-- Teams Card -->
                                <Border Grid.Row="1" Grid.Column="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="12,12,0,12">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                            <iconPacks:PackIconMaterial  Kind="AccountGroup" Width="24" Height="24" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Teams" Style="{StaticResource SubHeadingStyle}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding TeamsPercent}" FontSize="24" FontWeight="Bold" Foreground="#8B5CF6" Margin="0,0,0,8"/>
                                        <ProgressBar Height="8" Background="#E8E8E8" Foreground="#8B5CF6" Margin="0,0,0,8" Value="{Binding TeamsProgress}"/>
                                        <TextBlock Text="{Binding TeamsPercent}" Style="{StaticResource BodyStyle}"/>
                                    </StackPanel>
                                </Border>

                                <!-- Games Card -->
                                <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,12,0,0">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                            <iconPacks:PackIconMaterial  Kind="GamepadVariant" Width="24" Height="24" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Games" Style="{StaticResource SubHeadingStyle}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding GamesPercent}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" Margin="0,0,0,8"/>
                                        <ProgressBar Height="8" Background="#E8E8E8" Foreground="{StaticResource SuccessBrush}" Margin="0,0,0,8" Value="{Binding GamesProgress}"/>
                                        <TextBlock Text="{Binding GamesPercent}" Style="{StaticResource BodyStyle}"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Actions Section -->
                            <TextBlock Text="Quick Actions" Style="{StaticResource SubHeadingStyle}" Margin="0,32,0,16"/>
                            <StackPanel Orientation="Vertical">
                                <Button Style="{StaticResource PrimaryButtonStyle}" Click="OnViewStatisticsClicked" Height="40" Margin="0,0,0,12">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <iconPacks:PackIconMaterial  Kind="ChartBox" Width="18" Height="18" Foreground="White" Margin="0,0,8,0"/>
                                        <TextBlock Text="View Full Statistics" Foreground="White" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource PrimaryButtonStyle}" Click="OnBrowseDataClicked" Height="40" Margin="0,0,0,12">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <iconPacks:PackIconMaterial  Kind="FormatListBulleted" Width="18" Height="18" Foreground="White" Margin="0,0,8,0"/>
                                        <TextBlock Text="Browse Data" Foreground="White" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource SecondaryButtonStyle}" Click="OnAutoFetchAllDataClicked" Height="40" Margin="0,0,0,12">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <iconPacks:PackIconMaterial  Kind="Refresh" Width="18" Height="18" Margin="0,0,8,0"/>
                                        <TextBlock Text="Auto-Fetch All Data" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource SecondaryButtonStyle}" Click="OnSettingsClicked" Height="40">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <iconPacks:PackIconMaterial  Kind="Cog" Width="18" Height="18" Margin="0,0,8,0"/>
                                        <TextBlock Text="Settings" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>

                            <!-- Info Box -->
                            <Border Background="{StaticResource SecondaryBrush}" CornerRadius="8" Padding="16" Margin="0,32,0,0">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <iconPacks:PackIconMaterial  Kind="LightbulbOn" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="Dashboard Updates" FontWeight="Bold" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBlock Text="The statistics below update automatically every 5 seconds. Click 'Auto-Fetch All Data' in Settings to populate your database with rugby data from the API." Style="{StaticResource BodyStyle}"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- Data Tab -->
                <TabItem Style="{StaticResource ModernTabItem}" x:Name="DataTab">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <iconPacks:PackIconMaterial  Kind="FormatListBulleted" Width="16" Height="16" Margin="0,0,4,0"/>
                            <TextBlock Text="Data"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="24">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Data Type:" Style="{StaticResource TitleStyle}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <ComboBox x:Name="DataTypeCombo" Width="200" SelectionChanged="OnDataTypeChanged" Style="{StaticResource ModernComboBox}">
                                    <ComboBoxItem>Countries</ComboBoxItem>
                                    <ComboBoxItem>Seasons</ComboBoxItem>
                                    <ComboBoxItem>Leagues</ComboBoxItem>
                                    <ComboBoxItem>Teams</ComboBoxItem>
                                    <ComboBoxItem>Games</ComboBoxItem>
                                </ComboBox>
                                <Button Style="{StaticResource SecondaryButtonStyle}" Click="OnRefreshClicked" Margin="12,0,0,0" Padding="12,8">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <iconPacks:PackIconMaterial  Kind="Refresh" Width="14" Height="14" Margin="0,0,4,0"/>
                                        <TextBlock Text="Refresh" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                
                                <!-- Search and Filter Section (only visible for Teams/Leagues) -->
                                <Separator Width="1" Background="{StaticResource BorderBrush}" Margin="16,0"/>
                                
                                <TextBlock Text="Search:" Style="{StaticResource TitleStyle}" VerticalAlignment="Center" Margin="12,0,8,0"/>
                                <TextBox x:Name="SearchBox" Width="200" Height="36" TextChanged="OnSearchTextChanged" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="12,8" Foreground="{StaticResource TextBrush}"/>
                                
                                <CheckBox x:Name="FavoritesCheckBox" VerticalAlignment="Center" Margin="16,0,0,0" Checked="OnFavoritesFilterChanged" Unchecked="OnFavoritesFilterChanged" Foreground="{StaticResource TextBrush}">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial  Kind="Star" Width="14" Height="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="Favorites Only" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </CheckBox>
                            </StackPanel>
                        </Border>
                        
                        <Grid Grid.Row="1">
                            <Grid x:Name="DataGridContainer">
                                <!-- Countries DataGrid -->
                                <DataGrid x:Name="CountriesDataGrid" ItemsSource="{Binding GridData}" AutoGenerateColumns="False" Style="{StaticResource ModernDataGrid}" CellEditEnding="DataGrid_CellEditEnding" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="60"/>
                                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True" Width="400"/>
                                        <DataGridTextColumn Header="Code" Binding="{Binding Code}" IsReadOnly="True" Width="100"/>
                                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" IsReadOnly="True" Width="150"/>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <!-- Seasons DataGrid -->
                                <DataGrid x:Name="SeasonsDataGrid" ItemsSource="{Binding GridData}" AutoGenerateColumns="False" Style="{StaticResource ModernDataGrid}" CellEditEnding="DataGrid_CellEditEnding" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="60"/>
                                        <DataGridTextColumn Header="Year" Binding="{Binding Year}" IsReadOnly="True" Width="120"/>
                                        <DataGridTextColumn Header="Current" Binding="{Binding Current}" IsReadOnly="True" Width="120"/>
                                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" IsReadOnly="True" Width="150"/>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <!-- Leagues DataGrid -->
                                <DataGrid x:Name="LeaguesDataGrid" ItemsSource="{Binding GridData}" AutoGenerateColumns="False" Style="{StaticResource ModernDataGrid}" CellEditEnding="DataGrid_CellEditEnding" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Header="⭐" Width="50">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding Favorite}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                            <DataGridTemplateColumn.CellEditingTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding Favorite}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellEditingTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="60"/>
                                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True" Width="300"/>
                                        <DataGridTextColumn Header="Country" Binding="{Binding Country}" IsReadOnly="True" Width="120"/>
                                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" IsReadOnly="True" Width="150"/>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <!-- Teams DataGrid -->
                                <DataGrid x:Name="TeamsDataGrid" ItemsSource="{Binding GridData}" AutoGenerateColumns="False" Style="{StaticResource ModernDataGrid}" CellEditEnding="DataGrid_CellEditEnding" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Header="⭐" Width="50">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding Favorite}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                            <DataGridTemplateColumn.CellEditingTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding Favorite}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellEditingTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="60"/>
                                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True" Width="350"/>
                                        <DataGridTextColumn Header="Code" Binding="{Binding Code}" IsReadOnly="True" Width="100"/>
                                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" IsReadOnly="True" Width="150"/>
                                    </DataGrid.Columns>
                                </DataGrid>

                                <!-- Games DataGrid -->
                                <DataGrid x:Name="GamesDataGrid" ItemsSource="{Binding GridData}" AutoGenerateColumns="False" Style="{StaticResource ModernDataGrid}" CellEditEnding="DataGrid_CellEditEnding" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Home Team" Binding="{Binding Home}" IsReadOnly="True" Width="250"/>
                                        <DataGridTextColumn Header="Away Team" Binding="{Binding Away}" IsReadOnly="True" Width="250"/>
                                        <DataGridTextColumn Header="Date" Binding="{Binding Date}" IsReadOnly="True" Width="120"/>
                                        <DataGridTextColumn Header="Venue" Binding="{Binding Venue}" IsReadOnly="True" Width="300"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Grid>
                    </Grid>
                </TabItem>

                <!-- Watch Tab -->
                <TabItem Style="{StaticResource ModernTabItem}" x:Name="WatchTab">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <iconPacks:PackIconMaterial  Kind="MoviePlay" Width="16" Height="16" Margin="0,0,4,0"/>
                            <TextBlock Text="Watch"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.4*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="0.6*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Left Panel: Game List and Filters -->
                        <Grid Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" VerticalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <!-- Filter Section -->
                            <Expander Name="FilterExpander" IsExpanded="False" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,10" Background="{StaticResource BorderBrush}">
                                <Expander.Header>
                                    <Grid Width="{Binding ElementName=FilterExpander, Path=ActualWidth}" Margin="0,5,0,0" >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" Margin="5,5,0,12" VerticalAlignment="Bottom">
                                            <iconPacks:PackIconMaterial  Kind="DatabaseSearchOutline" Width="16" Height="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Filters" Style="{StaticResource SubHeadingStyle}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <CheckBox Grid.Column="1" IsChecked="{Binding ShowFavoritesOnly}" Foreground="{StaticResource TextBrush}" Margin="0,0,0,12" VerticalAlignment="Bottom">
                                            <StackPanel Orientation="Horizontal">
                                                <iconPacks:PackIconMaterial  Kind="Star" Width="14" Height="14" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Favorites Only" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </CheckBox>
                                    </Grid>
                                </Expander.Header>

                                <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="12,0,12,16">
                                    <StackPanel>
                                        
                                        <TextBlock Text="League:" Style="{StaticResource TitleStyle}" Margin="0,0,0,4"/>
                                        <ComboBox ItemsSource="{Binding Leagues}" SelectedValuePath="Id" SelectedValue="{Binding SelectedLeagueId}" DisplayMemberPath="Name" Style="{StaticResource ModernComboBox}" Margin="0,0,0,12" Height="36"/>
                                        <TextBlock Text="Season:" Style="{StaticResource TitleStyle}" Margin="0,0,0,4"/>
                                        <ComboBox ItemsSource="{Binding Seasons}" SelectedValue="{Binding SelectedSeasonId}" Style="{StaticResource ModernComboBox}" Margin="0,0,0,12" Height="36"/>
                                        <TextBlock Text="Team:" Style="{StaticResource TitleStyle}" Margin="0,0,0,4"/>
                                        <ComboBox ItemsSource="{Binding Teams}" SelectedValuePath="Id" SelectedValue="{Binding SelectedTeamId}" DisplayMemberPath="Name" Style="{StaticResource ModernComboBox}" Margin="0,0,0,12" Height="36"/>
                                        
                                        <Button Command="{Binding ClearFiltersCommand}" Style="{StaticResource SecondaryButtonStyle}" Padding="12,8">
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <iconPacks:PackIconMaterial  Kind="Close" Width="14" Height="14" Margin="0,0,4,0"/>
                                                <TextBlock Text="Clear Filters" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Command="{Binding LoadDataCommand}" Style="{StaticResource PrimaryButtonStyle}" Padding="12,8" Margin="0,12,0,0">
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <iconPacks:PackIconMaterial  Kind="Refresh" Width="14" Height="14" Foreground="White" Margin="0,0,4,0"/>
                                                <TextBlock Text="Load Games" Foreground="White" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Border>
                            </Expander>
                            <!-- Games List -->
                            <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="0" Margin="12,0,12,0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid>
                                        <ListBox Grid.Row="0" ItemsSource="{Binding FilteredGames}" SelectedItem="{Binding SelectedGame}" BorderThickness="0">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Padding="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                                                        <StackPanel>
                                                            <Border Background="{StaticResource BorderBrush}">
                                                            <TextBlock Text="{Binding LeagueName}" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,4" VerticalAlignment="Stretch"/>
                                                            </Border>
                                                            <TextBlock Text="{Binding HomeTeamName}" Margin="0,0,0,2"/>
                                                            <TextBlock Text="vs"/>
                                                            <TextBlock Text="{Binding AwayTeamName}" Margin="0,2,0,8"/>
                                                            <TextBlock Text="{Binding Date, StringFormat='yyyy-MM-dd'}" Style="{StaticResource BodyStyle}" Margin="0,0,0,4"/>
                                                            <TextBlock Text="{Binding VideoStatus}" Style="{StaticResource BodyStyle}" Foreground="{StaticResource AccentBrush}"/>
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Grid>
                                    <!-- Pagination Controls -->
                                    <Border Grid.Row="1" Background="#FAFAFA" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="12">
                                        <StackPanel>
                                            <!-- Page Size Selection -->
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,12">
                                                <TextBlock Text="Items per page:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
                                                <ComboBox ItemsSource="{Binding PageSizeOptions}" SelectedValue="{Binding PageSize}" Width="70" Height="28" Style="{StaticResource ModernComboBox}" Padding="8,4"/>
                                            </StackPanel>
                                            
                                            <!-- Navigation Controls -->
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button Command="{Binding FirstPageCommand}" Style="{StaticResource SecondaryButtonStyle}" Padding="10,6" FontSize="11" Margin="0,0,4,0">
                                                    <iconPacks:PackIconMaterial  Kind="SkipPrevious" Width="12" Height="12"/>
                                                </Button>
                                                <Button Command="{Binding PreviousPageCommand}" Style="{StaticResource SecondaryButtonStyle}" Padding="10,6" FontSize="11" Margin="0,0,4,0">
                                                    <iconPacks:PackIconMaterial  Kind="ChevronLeft" Width="12" Height="12"/>
                                                </Button>
                                                
                                                <Separator Width="1" Background="{StaticResource BorderBrush}" Margin="8,0"/>
                                                
                                                <TextBlock Text="Go to page:" VerticalAlignment="Center" Margin="8,0,4,0" FontWeight="SemiBold" FontSize="11"/>
                                                <ComboBox ItemsSource="{Binding PageNumbers}" SelectedValue="{Binding CurrentPage}" Width="60" Height="28" Style="{StaticResource ModernComboBox}" Padding="8,4" Margin="0,0,4,0"/>
                                                
                                                <Separator Width="1" Background="{StaticResource BorderBrush}" Margin="8,0"/>
                                                
                                                <Button Command="{Binding NextPageCommand}" Style="{StaticResource SecondaryButtonStyle}" Padding="10,6" FontSize="11" Margin="8,0,4,0">
                                                    <iconPacks:PackIconMaterial  Kind="ChevronRight" Width="12" Height="12"/>
                                                </Button>
                                                <Button Command="{Binding LastPageCommand}" Style="{StaticResource SecondaryButtonStyle}" Padding="10,6" FontSize="11">
                                                    <iconPacks:PackIconMaterial  Kind="SkipNext" Width="12" Height="12"/>
                                                </Button>
                                                
                                                <Separator Width="1" Background="{StaticResource BorderBrush}" Margin="8,0"/>
                                                
                                                <TextBlock VerticalAlignment="Center" Margin="8,0,0,0" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}">
                                                    <Run Text="{Binding CurrentPage, StringFormat='Page {0}'}"/><Run Text=" of "/><Run Text="{Binding TotalPages}"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- Status Message -->
                                    <Border Grid.Row="2" Background="#FAFAFA" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="12">
                                        <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource BodyStyle}"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </Grid>

                        <!-- Splitter -->
                        <GridSplitter Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Width="4" HorizontalAlignment="Center" Background="{StaticResource BorderBrush}"/>

                        <!-- Right Panel: Video List and Details -->
                        <StackPanel Grid.Column="2" Grid.Row="0" Grid.RowSpan="2">
                            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="12,0,0,16">
                                <StackPanel>
                                    <TextBlock Text="Videos" Style="{StaticResource SubHeadingStyle}" Margin="0,0,0,12"/>
                                    <Button Style="{StaticResource PrimaryButtonStyle}" Padding="12,8" Margin="0,0,0,12" Command="{Binding AddVideoCommand}">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <iconPacks:PackIconMaterial  Kind="Plus" Width="14" Height="14" Foreground="White" Margin="0,0,4,0"/>
                                            <TextBlock Text="Add Video" Foreground="White" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource SecondaryButtonStyle}" Padding="12,8" Margin="0,0,0,12" Command="{Binding EditVideoCommand}">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <iconPacks:PackIconMaterial  Kind="Pencil" Width="14" Height="14" Margin="0,0,4,0"/>
                                            <TextBlock Text="Edit Video" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource DangerButtonStyle}" Padding="12,8" Command="{Binding DeleteVideoCommand}">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <iconPacks:PackIconMaterial  Kind="TrashCan" Width="14" Height="14" Foreground="White" Margin="0,0,4,0"/>
                                            <TextBlock Text="Delete Video" Foreground="White" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Border>

                            <!-- Videos List -->
                            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="0" Margin="12,0,0,0">
                                <DataGrid ItemsSource="{Binding SelectedGameVideos}" AutoGenerateColumns="False" Style="{StaticResource ModernDataGrid}">
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Header="👁️" Width="40">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding IsWatched}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                            <DataGridTemplateColumn.CellEditingTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding IsWatched}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellEditingTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Header="⭐" Width="40">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding IsFavorite}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                            <DataGridTemplateColumn.CellEditingTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding IsFavorite}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellEditingTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" IsReadOnly="True" Width="200"/>
                                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat='yyyy-MM-dd'}" IsReadOnly="True" Width="80"/>
                                        <DataGridTemplateColumn Header="Rating" Width="140">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                        <!-- Star 1 -->
                                                        <Button Background="Transparent" BorderThickness="0" Padding="3" ToolTip="Rate 1 star" Click="RatingStar_Click" Tag="1">
                                                            <Button.Foreground>
                                                                <MultiBinding Converter="{StaticResource RatingStarColorConverter}">
                                                                    <Binding Path="Rating" />
                                                                    <Binding Source="1" />
                                                                </MultiBinding>
                                                            </Button.Foreground>
                                                            <iconPacks:PackIconMaterial Kind="Star" Width="18" Height="18" Margin="0,0,1,0"/>
                                                        </Button>
                                                        <!-- Star 2 -->
                                                        <Button Background="Transparent" BorderThickness="0" Padding="3" ToolTip="Rate 2 stars" Click="RatingStar_Click" Tag="2">
                                                            <Button.Foreground>
                                                                <MultiBinding Converter="{StaticResource RatingStarColorConverter}">
                                                                    <Binding Path="Rating" />
                                                                    <Binding Source="2" />
                                                                </MultiBinding>
                                                            </Button.Foreground>
                                                            <iconPacks:PackIconMaterial Kind="Star" Width="18" Height="18" Margin="0,0,1,0"/>
                                                        </Button>
                                                        <!-- Star 3 -->
                                                        <Button Background="Transparent" BorderThickness="0" Padding="3" ToolTip="Rate 3 stars" Click="RatingStar_Click" Tag="3">
                                                            <Button.Foreground>
                                                                <MultiBinding Converter="{StaticResource RatingStarColorConverter}">
                                                                    <Binding Path="Rating" />
                                                                    <Binding Source="3" />
                                                                </MultiBinding>
                                                            </Button.Foreground>
                                                            <iconPacks:PackIconMaterial Kind="Star" Width="18" Height="18" Margin="0,0,1,0"/>
                                                        </Button>
                                                        <!-- Star 4 -->
                                                        <Button Background="Transparent" BorderThickness="0" Padding="3" ToolTip="Rate 4 stars" Click="RatingStar_Click" Tag="4">
                                                            <Button.Foreground>
                                                                <MultiBinding Converter="{StaticResource RatingStarColorConverter}">
                                                                    <Binding Path="Rating" />
                                                                    <Binding Source="4" />
                                                                </MultiBinding>
                                                            </Button.Foreground>
                                                            <iconPacks:PackIconMaterial Kind="Star" Width="18" Height="18" Margin="0,0,1,0"/>
                                                        </Button>
                                                        <!-- Star 5 -->
                                                        <Button Background="Transparent" BorderThickness="0" Padding="3" ToolTip="Rate 5 stars" Click="RatingStar_Click" Tag="5">
                                                            <Button.Foreground>
                                                                <MultiBinding Converter="{StaticResource RatingStarColorConverter}">
                                                                    <Binding Path="Rating" />
                                                                    <Binding Source="5" />
                                                                </MultiBinding>
                                                            </Button.Foreground>
                                                            <iconPacks:PackIconMaterial Kind="Star" Width="18" Height="18" Margin="0,0,1,0"/>
                                                        </Button>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Border>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <!-- Settings Tab -->
                <TabItem Style="{StaticResource ModernTabItem}" x:Name="SettingsTab">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <iconPacks:PackIconMaterial  Kind="Cog" Width="16" Height="16" Margin="0,0,4,0"/>
                            <TextBlock Text="Settings"/>
                        </StackPanel>
                    </TabItem.Header>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
                        <StackPanel MaxWidth="600">
                            <TextBlock Text="Settings" Style="{StaticResource SubHeadingStyle}" Margin="0,0,0,24"/>
                            <GroupBox Header="API Configuration" Style="{StaticResource ModernGroupBox}" Margin="0,0,0,16">
                                <StackPanel>
                                    <TextBlock Text="API Key (from api-sports.io)" Style="{StaticResource TitleStyle}" Margin="0,0,0,8"/>
                                    <TextBox x:Name="ApiKeyBox" Margin="0,0,0,12" Height="36" Text="{Binding ApiKey, UpdateSourceTrigger=PropertyChanged}" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="12,8" FontSize="12" Foreground="{StaticResource TextBrush}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveApiKeyCommand}" Padding="16,10" Margin="0,0,8,0">
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <iconPacks:PackIconMaterial  Kind="ContentSave" Width="14" Height="14" Foreground="White" Margin="0,0,4,0"/>
                                                <TextBlock Text="Save API Key" Foreground="White" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Style="{StaticResource DangerButtonStyle}" Command="{Binding ClearApiKeyCommand}" Padding="16,10" Margin="0,0,8,0">
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <iconPacks:PackIconMaterial  Kind="Key" Width="14" Height="14" Foreground="White" Margin="0,0,4,0"/>
                                                <TextBlock Text="Clear API Key" Foreground="White" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Style="{StaticResource SecondaryButtonStyle}" Command="{Binding TestApiKeyCommand}" Padding="16,10">
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <iconPacks:PackIconMaterial  Kind="Beaker" Width="14" Height="14" Margin="0,0,4,0"/>
                                                <TextBlock Text="Test API Key" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                    <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource BodyStyle}" Margin="0,12,0,0" TextWrapping="Wrap" Foreground="{StaticResource TextBrush}"/>
                                </StackPanel>
                            </GroupBox>
                            <GroupBox Header="Database" Style="{StaticResource ModernGroupBox}" Margin="0,0,0,16">
                                <StackPanel>
                                    <Button Style="{StaticResource PrimaryButtonStyle}" Command="{Binding AutoFetchAllDataCommand}" Height="40" Margin="0,0,0,8">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <iconPacks:PackIconMaterial  Kind="Refresh" Width="16" Height="16" Foreground="White" Margin="0,0,8,0"/>
                                            <TextBlock Text="Auto-Fetch All Data" Foreground="White" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ShowDatabasePathCommand}" Height="40" Margin="0,0,0,8">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <iconPacks:PackIconMaterial  Kind="FolderOpen" Width="16" Height="16" Margin="0,0,8,0"/>
                                            <TextBlock Text="Show Database Path" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource DangerButtonStyle}" Command="{Binding ClearAllDataCommand}" Height="40">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <iconPacks:PackIconMaterial  Kind="TrashCan" Width="16" Height="16" Foreground="White" Margin="0,0,8,0"/>
                                            <TextBlock Text="Clear All Data" Foreground="White" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </GroupBox>
                            <GroupBox Header="About" Style="{StaticResource ModernGroupBox}">
                                <StackPanel>
                                    <TextBlock Text="Rugby API Manager v1.0" Style="{StaticResource TitleStyle}" Margin="0,0,0,8"/>
                                    <TextBlock Text="A professional application for managing rugby data from api-sports.io" Style="{StaticResource BodyStyle}"/>
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </DockPanel>
    </Grid>
</Window>
