<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RugbyApiApp.MAUI.Converters"
        xmlns:views="clr-namespace:RugbyApiApp.MAUI.Views"
        xmlns:iconPacks="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Material"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Class="RugbyApiApp.MAUI.MainWindow"
        Title="Rugby API Manager" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        FontFamily="Segoe UI"
        FontSize="12">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/SharedResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="163*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.RowSpan="2">
            <!-- Header -->
            <Border DockPanel.Dock="Top" Background="{StaticResource PrimaryBrush}" Padding="24,16">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <iconPacks:PackIconMaterial  Kind="Rugby"  Width="32" Height="32" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Rugby API Manager" Style="{StaticResource SubHeadingStyle}" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Content -->
            <TabControl Background="{StaticResource BackgroundBrush}" BorderThickness="0" x:Name="TabControl">
                <!-- Home Tab -->
                <TabItem Style="{StaticResource ModernTabItem}" x:Name="HomeTab">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <iconPacks:PackIconMaterial Kind="Home"  Width="16" Height="16" Margin="0,0,4,0"/>
                            <TextBlock Text="Home"/>
                        </StackPanel>
                    </TabItem.Header>
                    <views:HomeTabView />
                </TabItem>

                <!-- Data Tab -->
                <TabItem Style="{StaticResource ModernTabItem}" x:Name="DataTab">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <iconPacks:PackIconMaterial  Kind="FormatListBulleted" Width="16" Height="16" Margin="0,0,4,0"/>
                            <TextBlock Text="Data"/>
                        </StackPanel>
                    </TabItem.Header>
                    <views:DataTabView />
                </TabItem>

                <!-- Watch Tab -->
                <TabItem Style="{StaticResource ModernTabItem}" x:Name="WatchTab">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <iconPacks:PackIconMaterial  Kind="MoviePlay" Width="16" Height="16" Margin="0,0,4,0"/>
                            <TextBlock Text="Watch"/>
                        </StackPanel>
                    </TabItem.Header>
                    <views:WatchTabView />
                </TabItem>

                <!-- Settings Tab -->
                <TabItem Style="{StaticResource ModernTabItem}" x:Name="SettingsTab">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <iconPacks:PackIconMaterial  Kind="Cog" Width="16" Height="16" Margin="0,0,4,0"/>
                            <TextBlock Text="Settings"/>
                        </StackPanel>
                    </TabItem.Header>
                    <views:SettingsTabView />
                </TabItem>
            </TabControl>
        </DockPanel>
    </Grid>
</Window>
